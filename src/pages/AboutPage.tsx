import { Box, Typography } from "@mui/material";

function AboutPage() {
  return (

    <Box > 
      <Typography variant="h4">About Page</Typography>
      <Typography variant="body1">
        This is a sample project using Vite, React, MUI, Zustand, and React Query.
      </Typography>
    </Box>
  );
}

export default AboutPage;